<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/black</item>
    </style>

    <style name="NormalFabTheme" parent="Widget.Design.FloatingActionButton">
        <item name="android:layout_width">@dimen/fab_size</item>
        <item name="android:layout_height">@dimen/fab_size</item>
        <item name="android:elevation">@dimen/fab_elevation</item>
        <item name="android:translationZ">@dimen/fab_z</item>
    </style>

    <style name="SmallFabTheme" parent="Widget.Design.FloatingActionButton">
        <item name="android:layout_width">@dimen/fab_size_small</item>
        <item name="android:layout_height">@dimen/fab_size_small</item>
        <item name="android:elevation">@dimen/fab_elevation</item>
        <item name="android:scaleType">center</item>
        <item name="android:translationZ">@dimen/fab_z</item>
        <item name="fabSize">mini</item>
    </style>

</resources>
